class Solution:
    def gcdOfOddEvenSums(self, n: int) -> int:
        b=n*n
        a=n*(n+1)
        def gcd(a,b):
            if b==0:
                return a
            return gcd(b,a%b)
        return gcd(b, a)
